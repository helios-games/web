language: ruby
rvm:
  - 2.2.2
before_install:
  - gem update --system
  - gem --version
  - ssh-keyscan games.alexecollins.com >> ~/.ssh/known_hosts
script: bundler exec middleman deploy --build-before
cache: bundler
